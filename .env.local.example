# Convex deployment URL (get from `npx convex dev`)
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# fal.ai API key (for image generation)
FAL_KEY=your-fal-api-key

# Note: R2 credentials are set in Convex environment variables, not here.
# Run: npx convex env set R2_ACCESS_KEY_ID <key>
# Run: npx convex env set R2_SECRET_ACCESS_KEY <secret>
# Run: npx convex env set R2_ENDPOINT <endpoint>
# Run: npx convex env set R2_BUCKET <bucket-name>
